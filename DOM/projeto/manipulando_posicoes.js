$(document).ready(function () {
    // definindo a posicao de um elemento com OFFSET
    // $('form .field:eq(1)').offset()
    // $('form .field:eq(1)').position() retorna posicao baseada no parent
    // $('form .field:eq(1)').offset({top: 268, left: 303})
    // retorna posicao da scrollBar
    // $(document).scrollTop(300)